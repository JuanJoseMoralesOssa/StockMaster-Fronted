import GenericPage from '../generic_page/GenericPage'
import { expensePageConfig } from '../../config/expensePageConfig'

function ExpensePage() {
    return <GenericPage config={expensePageConfig} />
}

export default ExpensePage
