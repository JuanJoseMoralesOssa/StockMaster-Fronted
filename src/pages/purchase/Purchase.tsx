import GenericPage from '../generic_page/GenericPage'
import { purchasePageConfig } from '../../config/purchasePageConfig'

function PurchasePage() {
    return <GenericPage config={purchasePageConfig} />
}

export default PurchasePage
